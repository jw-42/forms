import { Theme } from '../interfaces/general';
export type Description = {
    category: string;
    type?: string;
    text: string;
};
type DescriptionList = {
    [Key in keyof Partial<Theme>]: Description;
};
declare const descriptions: DescriptionList;
export default descriptions;
