"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.replacePropDeep = void 0;
function replacePropDeep(object, mapReplace) {
    return Object.entries(object).reduce(function (acc, _a) {
        var key = _a[0], value = _a[1];
        if (typeof value === 'object' && !(key in mapReplace)) {
            acc[key] = replacePropDeep(value, mapReplace);
            return acc;
        }
        if (key in mapReplace) {
            acc[key] = mapReplace[key];
        }
        else {
            acc[key] = value;
        }
        return acc;
    }, {});
}
exports.replacePropDeep = replacePropDeep;
