"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.mixColors = void 0;
var color_1 = __importDefault(require("color"));
var mixColors = function (colorArg, colorState, opacity) {
    if (colorArg === 'transparent') {
        return (0, color_1.default)(colorState)
            .alpha(0.01 * Math.round(Math.min(1, opacity) * 100))
            .rgb()
            .string();
    }
    var colorObj = (0, color_1.default)(colorArg);
    var alpha = colorObj.alpha();
    if (alpha !== 1) {
        return colorObj
            .alpha(0.01 * Math.round(Math.min(1, alpha + opacity) * 100))
            .rgb()
            .string();
    }
    return colorObj.mix((0, color_1.default)(colorState), opacity).hex();
};
exports.mixColors = mixColors;
