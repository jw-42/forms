import { Theme, ThemeCssVarsWide } from '../../../interfaces/general';
export declare function getVariableName({ key, prefix, mode, }: {
    key: string;
    prefix?: string;
    mode?: any | 'auto';
}): string;
interface Options {
    valueConstruct?: (name: string, value?: string) => string;
}
/**
 * Процессор, который на основе темы, создаёт сложную структуру для CssVars темы
 */
export declare function extractVarsNames<T = Theme>(sourceTheme: T, opt?: Options): ThemeCssVarsWide<T>;
export {};
